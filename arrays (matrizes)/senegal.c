#include <stdio.h>
void calcular_aposta(int largura, int altura, int bandeira[altura][largura], char cor){
    
}
int main(){
    int largura;
    int altura;
    int bandeira[altura][largura];
    char cor;
    
    scanf("%d %d %c", &largura, &altura, &cor);
    printf("%d %d %c", largura, altura, cor); //Checando se está havendo a impressão da entrada corretamente
    
    return 0;
}